name: Secret Guard

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan tracked files for high-confidence secrets
        shell: pwsh
        run: .\scripts\secret-guard.ps1 -Mode all
